name: annotation_analyzer
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.17.1 <3.0.0'

dependencies:
  analyzer: any
  build: any
  build_config: any
  source_gen: any
  aa_annotation:
    path: ../aa_annotation/

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
